# MuJoCo 使用说明文档

## 简介

**MuJoCo**（Multi-Joint dynamics with Contact）是 Google DeepMind 开源的通用物理仿真引擎，广泛应用于机器人学、生物力学、图形动画和强化学习等领域。

本仓库包含：
- `source/` — google-deepmind/mujoco 完整源码（C/C++、Python绑定、MJX等）
- `software/` — MuJoCo 3.10.0 Linux aarch64 预编译二进制包及解压安装目录

---

## 环境要求

- 操作系统：Linux aarch64（如 Jetson Orin AGX）
- Python 3.8+
- CMake 3.16+（源码编译时需要）

---

## 安装步骤

### 方式一：使用预编译二进制包（推荐，快速）

```bash
# 1. 进入 software/ 目录
cd software/

# 2. 解压压缩包
tar -xzf mujoco-3.10.0-linux-aarch64.tar.gz

# 3. 将文件拷贝到 ~/.mujoco（或自定义路径）
mkdir -p ~/.mujoco
cp -r mujoco-3.10.0/* ~/.mujoco/

# 3. 配置环境变量（写入 ~/.bashrc）
echo 'export MUJOCO_PATH=~/.mujoco' >> ~/.bashrc
echo 'export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:~/.mujoco/lib' >> ~/.bashrc
echo 'export PATH=$PATH:~/.mujoco/bin' >> ~/.bashrc
source ~/.bashrc

# 4. 验证安装
simulate --version
```

### 方式二：从源码编译

```bash
# 进入源码目录
cd source/

# 安装依赖
sudo apt-get install -y cmake libgl1-mesa-dev libglu1-mesa-dev libxinerama-dev \
    libxcursor-dev libxrandr-dev libxi-dev libxxf86vm-dev

# 编译
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
make -j$(nproc)
sudo make install
```

### 安装 Python 绑定（mujoco-py / dm_control）

```bash
pip install mujoco
# 或安装 dm_control（DeepMind 控制套件）
pip install dm_control
```

---

## 目录结构说明

```
mujoco/
├── source/                     # google-deepmind/mujoco 完整源码
│   ├── src/                    # MuJoCo C/C++ 核心源码
│   ├── python/                 # Python 绑定源码
│   ├── mjx/                    # MuJoCo XLA（JAX 加速后端）
│   ├── model/                  # 内置标准模型
│   ├── simulate/               # 交互式可视化模拟器源码
│   ├── plugin/                 # 插件（SDF、弹性体、传感器等）
│   ├── include/                # C/C++ 头文件
│   ├── doc/                    # 文档源文件
│   ├── test/                   # 测试代码
│   ├── cmake/                  # CMake 构建配置
│   └── CMakeLists.txt          # 主构建文件
├── software/                   # 预编译二进制及安装包
│   ├── mujoco-3.10.0/          # 解压后的安装目录
│   │   ├── bin/                # 可执行文件（simulate 等）
│   │   ├── include/            # C/C++ 头文件
│   │   ├── lib/                # 动态/静态链接库
│   │   ├── model/              # 示例模型（XML）
│   │   └── sample/             # 示例代码
│   └── mujoco-3.10.0-linux-aarch64.tar.gz  # 原始压缩包
└── 使用说明文档.md              # 本文档
```

---

## 快速使用

### 1. 启动交互式模拟器

```bash
# 加载内置人形模型
simulate ~/.mujoco/model/humanoid/humanoid.xml

# 加载自定义模型
simulate /path/to/your/model.xml
```

交互操作：
- 鼠标左键拖拽：旋转视角
- 鼠标右键拖拽：平移视角
- 滚轮：缩放
- 空格键：暂停/继续仿真
- `Ctrl+R`：重置仿真

### 2. Python API 示例

```python
import mujoco
import mujoco.viewer

# 加载模型
model = mujoco.MjModel.from_xml_path('model.xml')
data = mujoco.MjData(model)

# 仿真一步
mujoco.mj_step(model, data)
print("时间:", data.time)
print("关节位置:", data.qpos)

# 启动可视化
with mujoco.viewer.launch_passive(model, data) as viewer:
    for _ in range(1000):
        mujoco.mj_step(model, data)
        viewer.sync()
```

### 3. 使用 MJX（JAX 加速）

```python
import jax
import mujoco
from mujoco import mjx

model = mujoco.MjModel.from_xml_path('model.xml')
mx = mjx.put_model(model)
dx = mjx.make_data(mx)

# JIT 编译加速
step_fn = jax.jit(mjx.step)
dx = step_fn(mx, dx)
```

---

## 常用模型文件位置

安装后内置模型位于 `~/.mujoco/model/` 或 `mujoco-3.10.0/model/`：

| 模型 | 路径 |
|------|------|
| 人形机器人 | `model/humanoid/humanoid.xml` |
| 手臂 | `model/arm26/arm26.xml` |
| 蚂蚁 | `model/ant/ant.xml` |
| 跑步机器人 | `model/cheetah/half_cheetah.xml` |

---

## 常见问题

**Q: 运行 simulate 报错 `libGL error`**
```bash
sudo apt-get install libgl1-mesa-glx libglu1-mesa
export LD_PRELOAD=/usr/lib/aarch64-linux-gnu/libGL.so.1
```

**Q: Python 导入 mujoco 失败**
```bash
pip install --upgrade mujoco
# 确保 LD_LIBRARY_PATH 包含 mujoco lib 路径
export LD_LIBRARY_PATH=$LD_LIBRARY_PATH:~/.mujoco/lib
```

**Q: 在 Jetson 上运行时 OpenGL 渲染问题**
```bash
# 使用 EGL 后端（无头渲染）
export MUJOCO_GL=egl
python your_script.py
```

---

## 参考资料

- 官方文档：https://mujoco.readthedocs.io/
- GitHub 仓库：https://github.com/google-deepmind/mujoco
- Python 绑定文档：https://mujoco.readthedocs.io/en/stable/python.html
- MJX 文档：https://mujoco.readthedocs.io/en/stable/mjx.html
